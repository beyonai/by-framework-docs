# Redis 注册中心配置
# 对应 RedisClient.getInstance() 加载逻辑

# Redis 主机地址，默认 localhost
gateway.redis.host=localhost

# Redis 端口，默认 6379
gateway.redis.port=6379

# Redis 数据库索引，默认 0
gateway.redis.db=0

# Redis 用户名 (可选，仅在开启 ACL 时使用)
gateway.redis.username=myuser

# Redis 密码 (可选)
gateway.redis.password=mypassword
