[project]
name = "by-plugins-sample"
version = "0.1.0"
description = "Plugin extension examples for By-Framework"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "by-framework",
    "langchain-core>=0.1.0",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
